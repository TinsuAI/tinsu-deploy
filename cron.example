# Nightly build + prod-data snapshot for the demo stack.
# Install on the box:  crontab -e  (user tinsu)
# Runs 02:30 box-local time; logs to ~/nightly-build.log (tinsu can't write /var/log).
30 2 * * * cd /home/tinsu/tinsu-deploy && ./scripts/nightly-build.sh >> /home/tinsu/nightly-build.log 2>&1
